version: '3'

services:
  portfoliomyriam:
    image: vmeijer/portfolio-myriam
    build:
      context: .
      dockerfile: PortfolioMyriam/Dockerfile
    environment:
      - PORTFOLIO_DB_HOST=
      - PORTFOLIO_DB_PORT=
      - PORTFOLIO_DB_USER=
      - PORTFOLIO_DB_PASSWORD=
      - ADMIN_PASSWORD=
      - GUEST_PASSWORD=
